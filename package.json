{
  "name": "inventory-manager",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "concurrently \"npm:dev --prefix api\" \"npm:dev --prefix client\"",
    "install:all": "npm install --prefix api && npm install --prefix client"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
